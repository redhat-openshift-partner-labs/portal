# Persistent storage for PostgreSQL data directory
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql
  labels:
    app.kubernetes.io/component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
